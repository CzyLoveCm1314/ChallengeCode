#include<cstdio>
#include<iostream>
#include<cstring>
using namespace std;

int t, n;
const int maxn = 3000;
int main() {
	scanf("%d", &t);
	while (t--) {
		scanf("%d", &n);
		for (int i = 0; i < n; i++)scanf("%d", &a[i]);
	}

	return 0;
}